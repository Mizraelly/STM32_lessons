#pragma once
#include <stdio.h>

void strcpy_and_reverse(const char *src, char *dst, long array_size);

long long u_sum (unsigned int* x, unsigned int array_length); 

long long sum (int* x, unsigned int array_length); 



